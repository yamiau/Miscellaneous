#include <stdlib.h>
#include <stdio.h>
#include <stdbool.h>

typedef struct node
{
    char[10] label;
    struct node = next;
} node;

node * newList(char[10] label);

int main()
{

}

node * newList(char[10] label)
{
    node * head = NULL;
    node *tail = NULL;

}

